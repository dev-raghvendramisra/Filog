import { ID } from 'appwrite'
import React from 'react'

function AlertModal({heading, message, feedbackMessage, primaryOnClick, secondaryOnClick, ctaDisabled=false, ctaDanger=false}) {
  return (
    <div id={ID.unique()+"alertModel-wrapper"}>
      
    </div>
  )
}

export default AlertModal